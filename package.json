{
  "name": "cryptocrier-ML",
  "packageManager": "yarn@3.1.1",
  "scripts": {
    "setup-venv": "python3 -m pip install --upgrade pip && python3 -m pip install virtualenv && python3 -m venv ./venv",
    "install-requirements": "python3 -m pip install --upgrade pip && python3 -m pip install -r requirements.txt",
    "flask:run": "python3 application.py"
  }
}
